# `assets`

This folder is reserved for static Raycast extension assets such as icons or packaged UI resources.

## Current State

- No meaningful project assets are currently checked in here.

## Notes

- Keep non-code extension resources here instead of mixing them into `src/`.
